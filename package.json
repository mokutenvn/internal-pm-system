{
  "name": "internal-project-management-system",
  "version": "1.0.0",
  "description": "Internal project management tool (Redmine-like) with goal setting & reports",
  "scripts": {
    "install-all": "cmd /c \"npm install --prefix backend && npm install --prefix frontend\"",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
